function calculadora(){
    numero = Number(prompt("Digite um número o qual deseja ver a tabuada: "))
}


programa
{
	
	funcao inicio()
	{
		inteiro numero, contador, limite, resultado
	
		escreva()
		leia(numero)

		escreva("Qual o limite da tabuada? ")
		leia(limite)

		escreva("\n")
		
		contador = 0

		faca{
			resultado = numero * contador
			escreva(numero + " x " + contador + " = " + resultado + "\n")
			contador++
		}enquanto(contador <= limite)
	}
}
